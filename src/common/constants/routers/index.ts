import { AUTH_URL } from "..";

export const PUBLIC_ROUTE = AUTH_URL;

// RBAC route
// USER ROUTE
export const USER_ROUTE = ["/antrian-anda", "/antrian", "/persyaratan"];

// ADMIN ROUTE
export const ADMIN_ROUTE = ["/statistik"];
